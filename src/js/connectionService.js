connectionModule = angular.module('boscouiApp.connectionService', []);


connectionModule.service('connectionService', ['$log', function($log) {
  
  var connections = new Array();
  
  this.getConnection(id) = function() {
    
  }
  
  
})
